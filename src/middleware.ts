// This file is kept for compatibility with Next.js but contains no authentication logic

export function middleware() {
  // All routes are public
  return;
}

export const config = {
  matcher: [],
}; 